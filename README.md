## React + React Mosaic + TS + Docker

Run project locally:

`git clone ` + path - clone project

`cd synerge_way` - move to project folder

`npm init` - initialize NPM;

`npm run dev` - start project locally

